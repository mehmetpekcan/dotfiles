# Touch and Interaction

- Use `touch-action: manipulation` where appropriate.
- Set `-webkit-tap-highlight-color` intentionally.
- Use `overscroll-behavior: contain` in modal/drawer/sheet contexts when needed.
- During drag interactions, avoid accidental text selection.
- Use `autoFocus` sparingly; avoid automatic focus on mobile.
