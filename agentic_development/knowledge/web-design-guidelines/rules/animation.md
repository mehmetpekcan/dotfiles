# Animation

- Honor `prefers-reduced-motion` with reduced or disabled animations.
- Animate `transform` and `opacity` where possible.
- Avoid `transition: all`; list transition properties explicitly.
- Set correct `transform-origin`.
- For SVG transforms, animate a wrapper group where needed.
- Animations should be interruptible and responsive to user input.
