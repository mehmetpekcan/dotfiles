# Navigation and State

- URL should reflect UI state (filters, tabs, pagination, expanded panels).
- Use `<a>`/`<Link>` for navigation to preserve expected browser behaviors.
- Deep-link stateful UI where practical.
- Destructive actions need confirmation or undo, not immediate irreversible execution.
