# Locale and Internationalization

- Format dates and times with `Intl.DateTimeFormat` (avoid hardcoded formats).
- Format numbers/currency with `Intl.NumberFormat` (avoid hardcoded formats).
- Detect language from browser/request language preferences, not IP.
