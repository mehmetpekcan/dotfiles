# Dark Mode and Theming

- Set `color-scheme: dark` on `html` for dark themes when appropriate.
- Keep `<meta name="theme-color">` aligned with effective page background.
- For native controls (such as `<select>`), set explicit colors for dark mode compatibility.
